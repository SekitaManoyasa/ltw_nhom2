@model EcommerceStore_Nhóm2.Models.ViewModel.ProductDetailsVM
@using PagedList.Mvc

<link rel="stylesheet" type="text/css" href="~/Content/MyStyle/CustomerHome.css" />

@if (Model.RelatedProducts != null && Model.RelatedProducts.Any())
{
    <section class="container-fluid featureProd">
        <h4 style="font-weightLbold;">Sản phẩm tượng tự</h4>
        <ul class="feature-container">
            @foreach (var product in Model.RelatedProducts)
            {
                <li class="fproduct-item">
                    <div>
                        <a href="@Url.Action("productDetails", "Home", new { id = product.ProductID })">
                            <img src="@Html.DisplayFor(modelItem => product.ProductImage)" />
                        </a>
                    </div>
                    <div>
                        <h5>@Html.DisplayFor(modelItem => product.ProductName)</h5>
                        <p class="prodSale">@Html.DisplayFor(modeItem => product.ProductPrice) </p>
                    </div>
                </li>
            }
        </ul>
    </section>
}
